#!/usr/bin/env python
"""
Minimal setup.py file for backward compatibility with editable installs.
All configuration is now in pyproject.toml.
"""
from setuptools import setup

if __name__ == "__main__":
    setup()
